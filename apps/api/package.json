{
  "name": "api",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "dev": "pnpm --filter places-core build && tsc -p tsconfig.build.json && node ./scripts/dev-watch.mjs",
    "start": "node dist/server.js",
    "test": "vitest run",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "places-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "26.2.0",
    "typescript": "catalog:",
    "vitest": "catalog:"
  }
}
